//create function and 4 parameter for and devide for multiplication
function avg1(house1, house2, house3, house4) {
  multiplication = house1 * house2 * house3 * house4;
  result = multiplication / 4;
  console.log(result);
}
house1 = 10;
house2 = 20;
house3 = 30;
house4 = 40;
avg1(house1, house2, house3, house4); //60000

//create function and 4 parameter for and devide for multiplication
function avg(house1, house2, house3, house4) {
  addition = house1 + house2 + house3 + house4;
  result1 = addition / 4;
  console.log(result);
}

//multiplying 4 numbers and find their average
multiplication = house1 * house2 * house3 * house4;
result = multiplication / 4;
console.log(result);

//subtraction 2n numbers and find their modulas
pos = house4 + house3;
neg = house1 + house2;
res = pos - neg;
console.log(res);
modulas = res % 4;
console.log(modulas);

house1 = 10;
house2 = 20;
house3 = 30;
house = 40;
avg(house1, house2, house3, house4);

//create a function, multiply first 2 numbers and devide by last number
function mul(a, b, c) {
  multi = a * b;
  result = multi / c;
  console.log(result);
}

mul(30, 25, 2);
fractice x = 30;
y = x;
console.log(x);
