//write a program to find dublicate characters in a string
const repeatchars = (myStr) => {
  for (let j = i + 1; j < myStr.length; j++) {
    if (myStr[i] == myStr[j]) {
      console.log(myStr[i]);
    }
  }
};
return -1;
repeatcharts("hello world");

//amother

let example = "hello world";
function charRepeats(str) {
  for (let i = 0; i < str.length; i++) {
    if (str.index0f(str[i]) !== str.lastindex0f(str[i])) [console.log(str[i])];
  }
}
return "There are no dublicate letters in the string";
console.log(charRepeats(example));
